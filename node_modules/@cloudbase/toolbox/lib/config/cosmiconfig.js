"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadConfig = exports.searchConfig = void 0;
const explorer_1 = require("./explorer");
const error_1 = require("../error");
const fs_1 = require("../fs");
const MODULE_NAME = 'cloudbase';
const getSearchPlaces = (moduleName) => [
    'package.json',
    `${moduleName}rc`,
    `${moduleName}rc.json`,
    `${moduleName}rc.yaml`,
    `${moduleName}rc.yml`,
    `${moduleName}rc.js`,
    `${moduleName}.config.js`
];
function searchConfig(dest, moduleName = MODULE_NAME) {
    return __awaiter(this, void 0, void 0, function* () {
        const explorer = explorer_1.cosmiconfig(moduleName, {
            stopDir: process.cwd(),
            searchPlaces: getSearchPlaces(moduleName)
        });
        try {
            return explorer.search(dest || process.cwd());
        }
        catch (e) {
            return null;
        }
    });
}
exports.searchConfig = searchConfig;
function loadConfig(options = {}) {
    return __awaiter(this, void 0, void 0, function* () {
        const { moduleName = MODULE_NAME, configPath, searchFrom } = options;
        const explorer = explorer_1.cosmiconfig(moduleName, {
            stopDir: process.cwd(),
            searchPlaces: getSearchPlaces(moduleName)
        });
        if (configPath) {
            fs_1.checkReadable(configPath, true);
            try {
                const result = yield explorer.load(configPath);
                if (!result)
                    return null;
                const { config, filepath, isEmpty } = result;
                return config;
            }
            catch (e) {
                throw new error_1.CloudBaseError('配置文件解析失败，请检查你的配置文件格式是否正确！', {
                    original: e
                });
            }
        }
        try {
            const result = yield searchConfig(searchFrom || process.cwd(), moduleName);
            if (!result)
                return null;
            const { config, filepath, isEmpty } = result;
            return config;
        }
        catch (e) {
            throw new error_1.CloudBaseError('配置文件解析失败，请检查你的配置文件格式是否正确！', {
                original: e
            });
        }
    });
}
exports.loadConfig = loadConfig;
