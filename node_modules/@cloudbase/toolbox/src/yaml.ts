// https://github.com/nodeca/js-yaml
import YAML from 'yaml';

// 解析 YAML 字符串
export const yamlParse = (doc: string) => {
    return YAML.parse(doc);
};

// 导出为 YAML 字符串
export const yamlStringify = (object: any) => {
    return YAML.stringify(object);
};

export const yaml = YAML;
